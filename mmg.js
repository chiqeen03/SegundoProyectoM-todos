function mmg(llegadas, servicios, servidores){
    let tasaLlegadas = llegadas;
    let tasaServicios = servicios;
    let s = servidores

    let p0;
    let p;
    let Lq;
    let L;
    let Wq;
    let W;

    // p
    p = tasaLlegadas/tasaServicios;

    // P0
    p0 = 1-p;

    //Lq = ((Math.pow(tasaLlegadas, 2) * ))/(2*(1-p))
}